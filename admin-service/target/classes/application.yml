server:
  port: 8082
  
spring:
  application:
    name: admin-service  
  datasource:
    url: jdbc:mysql://localhost:3306/store
    username: root
    password: $1Welcome
  jpa:
    hibernate:
      ddl:
        auto: update 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka